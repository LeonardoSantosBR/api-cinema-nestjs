version: '3.8'

services:
  api:
    build:
      context: .
    image: leonardosantosdemelo/api-cinema:v4
    container_name: api-v4
    env_file:
      - .env
    ports:
      - '3000:3000'
    command: ['node', 'dist/main.js']
